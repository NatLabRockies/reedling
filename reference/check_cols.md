# Check if required columns are present

Check if required columns are present

## Usage

``` r
check_cols(df, cols, fail = T)
```

## Arguments

- df:

  dataframe or datatable object

- cols:

  vector of column names to check

- stop:

  boolean to stop the script if check fails
